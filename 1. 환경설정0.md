# 환경설정0

## 순서

1. WSL2 설치
2. Ubuntu 22.04 LTS 설치
3. DeepRacer 로컬 학습환경 구축 → `환경설정1.md`(기본적으로는 환경설정1_docker버전.md을 사용합니다.)

---

## 1. WSL2 설치

### 1. PowerShell을 관리자 권한으로 실행

Windows에서 **PowerShell**을 검색한 후:

```text
관리자 권한으로 실행
```

을 선택한다.

### 2. WSL 설치

```powershell
wsl --install
```

설치 완료 후 재부팅을 요구한다면 Windows를 재부팅한다.

### 3. WSL 상태 확인

```powershell
wsl --status
```

---

## 2. Ubuntu 22.04 LTS 설치

### 1. 현재 설치된 Ubuntu 확인

```powershell
wsl -l -v
```

이미 다음과 같이 `Ubuntu-22.04`가 있고 `VERSION`이 `2`라면 새로 설치할 필요가 없다.

```text
NAME            STATE      VERSION
Ubuntu-22.04    Stopped    2
```

### 2. 설치 가능한 Ubuntu 확인

```powershell
wsl --list --online
```

목록에서 다음을 찾는다.

```text
Ubuntu-22.04
```

### 3. Ubuntu 22.04 LTS 설치

```powershell
wsl --install -d Ubuntu-22.04
```

### 4. Ubuntu 초기 설정

Ubuntu를 처음 실행하면 사용자 이름과 비밀번호를 설정한다.

```text
Enter new UNIX username:
New password:
Retype new password:
```

> 비밀번호 입력 중에는 화면에 글자나 `*`가 표시되지 않는다. 정상적인 동작이다.

### 5. Ubuntu 버전 확인

Ubuntu 터미널:

```bash
lsb_release -a
```

다음과 같이 `22.04`가 나오면 정상이다.

```text
Distributor ID: Ubuntu
Description:    Ubuntu 22.04.x LTS
Release:        22.04
Codename:       jammy
```

### 6. WSL2 확인

Windows PowerShell:

```powershell
wsl -l -v
```

정상 예:

```text
NAME            STATE      VERSION
Ubuntu-22.04    Running    2
```

`VERSION`이 `2`이면 정상이다.

만약 `1`이라면:

```powershell
wsl --set-version Ubuntu-22.04 2
```

---

## 3. DeepRacer 로컬 학습환경 구축

다음 단계는:

> `환경설정1.md`

를 참고한다.

---

# 참고

## Ubuntu 22.04 직접 실행

PowerShell:

```powershell
wsl -d Ubuntu-22.04
```

또는 Windows 시작 메뉴에서 `Ubuntu 22.04`를 검색하여 실행한다.

## Ubuntu 22.04를 기본 배포판으로 설정

Ubuntu가 여러 개 설치되어 있다면:

```powershell
wsl --set-default Ubuntu-22.04
```

확인:

```powershell
wsl -l -v
```

기본 배포판 앞에는 `*`가 표시된다.

```text
  NAME            STATE      VERSION
* Ubuntu-22.04    Running    2
  Ubuntu-20.04    Stopped    2
```