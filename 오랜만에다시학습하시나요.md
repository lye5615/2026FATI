# FATI DeepRacer for Cloud 오랜만에 다시 학습하기

```text
노트북을 완전히 종료했다.
며칠 뒤 다시 학습하려고 한다.
Windows를 재부팅했다.
WSL을 종료했다가 다시 켰다.
Docker Desktop을 종료했다가 다시 실행했다.
```
# 요약

```bash
설치한 Ubuntu 22.04를 검색하여 바로 실행할 수 있습니다.

실행 후

cd ~/deepracer-for-cloud

source bin/activate.sh

docker info

docker service ls

dr-summary

dr-start-training -w
```

새 모델 prefix라면:

```bash
dr-start-training
```

학습 확인:

```bash
docker service ls
```

```text
Docker 시작
→ WSL 실행
→ DRfC 활성화
→ Docker/Swarm/MinIO 상태 확인
→ 기존 학습 서비스 정리
→ 모델 존재 확인
→ 트랙 및 prefix 확인
→ 학습 재시작
```

순서로 진행한다.

---

# 1. Docker Desktop 실행

Windows에서:

```text
Docker Desktop
```

을 실행한다.

Docker Engine이 정상적으로 실행 중인지 확인한다.

---

# 2. WSL Integration 확인

Docker Desktop:

```text
Settings
→ Resources
→ WSL Integration
```

다음을 확인한다.

```text
Enable integration with my default WSL distro → ON
Ubuntu-22.04 → ON
```

필요하다면:

```text
Apply & Restart
```

를 누른다.

---

# 3. Ubuntu 22.04 실행

PowerShell:

```powershell
wsl -d Ubuntu-22.04
```

---

# 4. DRfC 폴더로 이동

Ubuntu:

```bash
cd ~/deepracer-for-cloud
```

---

# 5. Docker 연결 확인

```bash
docker --version
```

그리고:

```bash
docker info
```

정상적으로 Docker 정보가 표시되어야 한다.

다음 오류가 나온다면:

```text
The command 'docker' could not be found in this WSL 2 distro.
```

> `에러슈팅.md`의 **1. docker: command not found** 참고

---

# 6. DRfC 환경 활성화

```bash
source bin/activate.sh
```

확인:

```bash
type dr-summary
```

정상:

```text
dr-summary is a function
```

`dr-*` 명령어가 계속 인식되지 않는다면:

> `에러슈팅.md`의 **4. dr-* 명령어가 인식되지 않는 경우** 참고

---

# 7. Docker Swarm 확인

```bash
docker node ls
```

정상:

```text
STATUS          Ready
AVAILABILITY    Active
MANAGER STATUS  Leader
```

즉:

```text
Ready
Active
Leader
```

인지 확인한다.

Swarm 자체에 문제가 있다면:

> `에러슈팅.md`의 **3. Docker Swarm 관련 오류** 참고

---

# 8. Docker 서비스 확인

```bash
docker service ls
```

학습이 실행되지 않는 정상 상태에서는 보통:

```text
s3_minio   1/1
```

만 존재한다.

---

# 9. 학습 서비스가 0/1로 남아 있는지 확인

예:

```text
deepracer-0_rl_coach      0/1
deepracer-0_robomaker     0/1
s3_minio                  1/1
```

이 상태라면 노트북 종료 전 실행하던 Swarm 서비스가 남아 있는 것일 수 있다.

정리:

```bash
dr-stop-training
```

다시 확인:

```bash
docker service ls
```

정상:

```text
s3_minio   1/1
```

자세한 해결 방법:

> `04_에러슈팅.md`의 **6. 재부팅 후 학습 서비스가 0/1인 경우** 참고

---

# 10. DRfC 전체 상태 확인

```bash
dr-summary
```

학습 미실행 상태라면 학습용:

```text
deepracer-0_rl_coach
deepracer-0_robomaker
```

서비스가 없어야 한다.

---

# 11. 기존 학습 모델 확인

MinIO의 모델 목록:

```bash
aws $DR_LOCAL_PROFILE_ENDPOINT_URL s3 ls s3://$DR_LOCAL_S3_BUCKET/
```

예:

```text
PRE custom_files/
PRE rl-deepracer-sagemaker/
PRE rl-deepracer-sagemaker-new/
```

이전 모델 prefix가 존재하는지 확인한다.

---

# 12. 모델 내부 확인

예를 들어:

```bash
aws $DR_LOCAL_PROFILE_ENDPOINT_URL s3 ls \
s3://$DR_LOCAL_S3_BUCKET/rl-deepracer-sagemaker/
```

정상 예:

```text
PRE ip/
PRE metrics/
PRE model/
PRE output/
PRE training-simtrace/

reward_function.py
training_params.yaml
```

다음이 보이면 학습 결과가 저장되어 있다.

```text
model/
metrics/
training-simtrace/
```

---

# 13. 실제 모델 파일 확인

```bash
aws $DR_LOCAL_PROFILE_ENDPOINT_URL s3 ls \
s3://$DR_LOCAL_S3_BUCKET/rl-deepracer-sagemaker/model/ \
--recursive
```

예:

```text
.coach_checkpoint
.ready

0_Step-0.ckpt.index
0_Step-0.ckpt.meta
0_Step-0.ckpt.data-00000-of-00001

model_metadata.json
deepracer_checkpoints.json

model_0.pb
model_1.pb
...
```

이러한 파일이 보이면 기존 학습 결과가 남아 있는 것이다.

---

# 14. 이번에 사용할 모델 Prefix 확인

`run.env` 확인:

```bash
nano run.env
```

예:

```bash
DR_LOCAL_S3_MODEL_PREFIX=rl-deepracer-sagemaker
```

---

# 15. 기존 Prefix를 그대로 사용하는 경우

이미 MinIO에 해당 모델 prefix가 존재한다면:

```bash
dr-start-training -w
```

를 사용한다.

일반:

```bash
dr-start-training
```

을 사용했을 때:

```text
Selected path ... exists.
Delete it, or use -w option.
```

오류가 나오는 것은 정상이다.

자세한 내용:

> `04_에러슈팅.md`의 **5. Selected path exists 오류** 참고

---

# 16. 새 Prefix를 사용하는 경우

`run.env`에서 새로운 이름으로 설정한다.

예:

```bash
DR_LOCAL_S3_MODEL_PREFIX=rl-deepracer-sagemaker-new3
```

반영:

```bash
dr-update-env
```

확인:

```bash
dr-summary
```

학습 시작:

```bash
dr-start-training
```

---

# 17. 학습 시작 후 확인

```bash
docker service ls
```

정상적인 학습 상태:

```text
deepracer-0_rl_coach      1/1
deepracer-0_robomaker     1/1
s3_minio                  1/1
```

전체 상태:

```bash
dr-summary
```

로그:

```bash
dr-logs-sagemaker
```

주행 로그:

```bash
dr-logs-robomaker
```

이후 학습 종료 및 모델 제출은 3. 샘플코드학습.md 파일과 4. 레이스참여.md 파일을 참고하세요:)