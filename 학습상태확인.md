# 학습 상태 확인

현재 학습이 잘 되고 있는지 확인하고 싶다면 이 파일을 참고하세요:)

#  1. 학습 서비스 확인

```bash
docker service ls
```

정상적인 학습 중 상태:

```text
deepracer-0_rl_coach      1/1
deepracer-0_robomaker     1/1
s3_minio                  1/1
```

각각:

```text
rl_coach
→ 강화학습 모델 학습

robomaker
→ 시뮬레이션 주행

s3_minio
→ 모델 및 로그 저장
```

역할을 한다.

`0/1` 상태가 지속되면:

> `에러슈팅.md`의 **6. 학습 서비스가 0/1인 경우** 참고

---

# 2. 전체 학습 상태 확인

```bash
dr-summary
```

Docker 서비스:

```bash
docker service ls
```

---

# 3. 학습 로그 확인(새로운 터미널 창을 켭니다.)

SageMaker/Coach 로그:

```bash
dr-logs-sagemaker
```

RoboMaker 주행 로그:

```bash
dr-logs-robomaker
```

로그가 길게 출력되는 것은 정상이다.

로그 화면에서 빠져나오려면:

```text
Ctrl + C
```

를 누른다.

중요:

```text
Ctrl + C
```

로 로그 출력을 종료해도 학습 서비스 자체는 계속 실행된다.

---

# 4. 학습 로그 읽는 법

예:

```text
Training> Name=main_level/agent, Worker=0, Episode=1,
Total reward=14.77, Steps=77, Training iteration=0
```

의미:

```text
Episode=1
→ 첫 번째 에피소드

Total reward=14.77
→ 해당 에피소드에서 받은 총 보상

Steps=77
→ 해당 에피소드의 Step 수

Training iteration=0
→ 현재 학습 iteration

Worker=0
→ 첫 번째 simulation worker
```

초기에는 reward가 낮거나 step이 짧아도 이상하지 않다.

여러 episode를 보면서 다음을 확인한다.

```text
Total reward가 증가하는가?
Steps가 증가하는가?
Training iteration이 증가하는가?
Progress가 증가하는가?
완주가 발생하는가?
```

---

# 5. RoboMaker SIM_TRACE_LOG 읽기

예:

```text
SIM_TRACE_LOG:39,110,...,17.71,...,in_progress,...
```

주요 의미:

```text
39
→ Episode

110
→ Step

17.71
→ Track Progress 약 17.71%

in_progress
→ 에피소드 진행 중
```

따라서 대략:

```text
Episode 39
Step 110
Progress 17.71%
```

로 볼 수 있다.

---

# 6. 완주 여부 확인

Progress가 99.9 이상인 로그 확인:

```bash
dr-logs-robomaker | grep --line-buffered "SIM_TRACE_LOG" | \
awk -F',' '$15 >= 99.9 {print}'
```

또는:

```bash
dr-logs-robomaker | grep --line-buffered -Ei \
"lap_complete|episode_complete|finished"
```

---

# 7. 언제 학습을 종료할까?

다음 지표를 본다.

```text
Total reward가 지속적으로 개선되는가?
Steps가 늘어나는가?
Progress가 높아지는가?
완주 Episode가 발생하는가?
완주가 반복적으로 발생하는가?
여러 iteration 동안 성능이 정체되는가?
```

완주가 안정적으로 나타나거나 성능 개선이 정체되면 종료를 고려한다.

---

# 8. 학습 중 노트북 주의사항

학습은 로컬 Docker + WSL 환경에서 수행된다.

따라서 다음 상태에서는 학습이 계속되지 않는다.

```text
절전
최대 절전
Windows 종료
재부팅
```

화면만 꺼지는 것은 괜찮지만 시스템 자체는 깨어 있어야 한다.

학습 중 권장:

```text
전원 연결
절전 사용 안 함
Docker Desktop 실행 유지
```

Docker 이미지 다운로드가 이미 완료되었다면 로컬 학습 자체에는 인터넷이 항상 필요한 것은 아니다.

---

# 9. 저장된 학습 모델 확인

환경변수 확인:

```bash
echo "$DR_LOCAL_PROFILE_ENDPOINT_URL"
echo "$DR_LOCAL_S3_BUCKET"
```

예:

```text
--profile minio --endpoint-url http://localhost:9000
bucket
```

MinIO 확인:

```bash
aws $DR_LOCAL_PROFILE_ENDPOINT_URL s3 ls s3://$DR_LOCAL_S3_BUCKET/
```

예:

```text
PRE custom_files/
PRE rl-deepracer-sagemaker/
```

`rl-deepracer-sagemaker/`가 모델 prefix이다.

---

# 10. 모델 내부 확인

```bash
aws $DR_LOCAL_PROFILE_ENDPOINT_URL s3 ls \
s3://$DR_LOCAL_S3_BUCKET/rl-deepracer-sagemaker/
```

예:

```text
PRE ip/
PRE metrics/
PRE model/
PRE output/
PRE training-simtrace/

reward_function.py
training_params.yaml
```

주요 폴더:

```text
metrics/
→ 학습 지표

model/
→ 실제 모델 및 checkpoint

training-simtrace/
→ 주행 로그

reward_function.py
→ 사용한 reward function

training_params.yaml
→ 해당 학습의 설정
```