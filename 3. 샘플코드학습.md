# FATI DeepRacer for Cloud 학습 및 모델 제출 가이드

이 문서는 환경설정0, 환경설정1이 이미 끝난 상태에서:

# 순서

1. DRfC 폴더로 이동 및 환경 활성화
2. 현재 설정 및 학습 트랙 확인
3. 학습용 파일 확인
4. 학습 파일 업로드
5. 학습 시작
6. 학습 종료

만약 며칠 후 또는 노트북 재부팅 후 다시 시작하는 경우:

> `오랜만에다시학습하시나요.md`

---

# 1. DRfC 폴더로 이동

```bash
cd ~/deepracer-for-cloud
```

환경 활성화:

```bash
source bin/activate.sh
```

---

# 2. 현재 설정 확인(중요! 처음 학습시키는 경우: 트랙이 'reInvent2019_track'인지 확인)

```bash
dr-summary
```

특히 확인할 항목:

```text
Docker style
Cloud / Bucket
Workers
Race type
World / track
Model prefix
```

대회용 트랙 `reInvent2019_track`:

```text
World / track    reInvent2019_track
```

인지 반드시 확인한다.

트랙 변경이 필요하다면 `run.env`를 수정한다.

```bash
nano run.env
```

예:

```bash
DR_WORLD_NAME=reInvent2019_track
```

저장 후:

```bash
dr-update-env
dr-summary
```

---

# 3. 학습용 파일 확인

```bash
ls custom_files
```

기본적으로 다음 파일이 필요하다.

```text
reward_function.py
model_metadata.json
hyperparameters.json
```

필요하다면 학습 전에 각 파일을 수정한다.
(수정없이 학습시켜도 무방하긴하다. 고도화를 원하는 경우 수정)

---

# 4. 학습 파일 업로드

```bash
dr-upload-custom-files
```

다음 파일이 Local MinIO로 업로드된다.

```text
reward_function.py
model_metadata.json
hyperparameters.json
```

업로드 위치:

```text
s3://bucket/custom_files/
```

---

# 5. 학습 시작

새로운 prefix로 처음 학습하는 경우:

```bash
dr-start-training
```

기존 모델 prefix가 이미 존재하면서 그대로 사용하려면:

```bash
dr-start-training -w
```

일반 `dr-start-training` 실행 시:

```text
Selected path ... exists.
Delete it, or use -w option.
```

오류가 발생한다면:

> `04_에러슈팅.md`의 **5. Selected path exists 오류** 참고

---
- 잘 학습되고 있는지 궁금하다면 '학습상태확인.md' 파일을 참고하세요.

# 6. 학습 종료

정상적으로 종료하려면:

```bash
dr-stop-training
```

출력 예:

```text
Stopping service ...
Removing service deepracer-0_rl_coach
Removing service deepracer-0_robomaker
```

종료 후:

```bash
docker service ls
```

확인한다.

정상:

```text
s3_minio   1/1
```

즉 학습용:

```text
deepracer-0_rl_coach
deepracer-0_robomaker
```

서비스가 사라져야 한다.

---

# 학습 과정 명령어 요약

```text
cd ~/deepracer-for-cloud

source bin/activate.sh

dr-summary

dr-upload-custom-files

dr-start-training
또는
dr-start-training -w

docker service ls

dr-logs-sagemaker
dr-logs-robomaker

dr-stop-training

모델 확인

model/만 다운로드

tar.gz 압축

제출
```