# DRfC WSL2 GPU 로컬 설치 가이드

이 문서는 Windows 11 + WSL2 Ubuntu + NVIDIA GeForce RTX 4060 Laptop GPU 환경에서 DeepRacer-for-Cloud(DRfC)를 GPU 및 Local MinIO 모드로 설치하고, 기본 학습을 시작하기까지의 절차를 정리한 체크리스트다.

## 전체 설치 흐름

1. 사전 환경 확인
2. 기본 패키지 설치
3. Docker Engine 설치
4. NVIDIA Container Toolkit 설치
5. Docker NVIDIA runtime 구성
6. Docker 권한 설정
7. CUDA 컨테이너 GPU 검증
8. Docker Swarm 사전 확인
9. DRfC 다운로드
10. DRfC GPU·Local 초기화
11. MinIO 프로필 확인
12. DRfC 환경 활성화
13. DRfC 인프라 확인
14. Custom files 검증
15. MinIO 업로드 및 첫 학습

> 중요: WSL Ubuntu 안에 `nvidia-driver-*`, `cuda-drivers`를 설치하지 않는다. NVIDIA driver는 Windows에만 설치하고, WSL에는 NVIDIA Container Toolkit만 설치한다.

---

## 0. 사전 환경 확인

WSL Ubuntu에서 다음을 실행한다.

```bash
cat /etc/os-release
uname -m
nvidia-smi
df -h /
free -h
```

확인 기준:

- 아키텍처가 `x86_64`인지 확인한다.
- `nvidia-smi`에 `NVIDIA GeForce RTX 4060 Laptop GPU`가 표시되는지 확인한다.
- 디스크 여유 공간은 최소 40GB, 가능하면 60GB 이상을 권장한다.
- 시스템 RAM과 GPU VRAM 합계는 가능하면 32GB 이상을 권장한다.

WSL에서 실행되는 `nvidia-smi`의 좌측 `NVIDIA-SMI` 버전은 Windows와 다를 수 있다. 실제 driver 판단에는 중앙의 `Driver Version`을 사용한다.

```bash
nvidia-smi \
  --query-gpu=driver_version \
  --format=csv,noheader
```

WSL용 실행 파일 경로도 확인한다.

```bash
command -v nvidia-smi
```

일반적인 정상 경로:

```text
/usr/lib/wsl/lib/nvidia-smi
```

---

## 1. 기본 패키지 설치

```bash
sudo apt-get update

sudo apt-get install -y \
  ca-certificates \
  curl \
  gnupg \
  git \
  jq \
  awscli \
  python3 \
  python3-boto3
```

설치 확인:

```bash
git --version
jq --version
aws --version
python3 --version
```

---

## 2. Docker Engine 설치

먼저 설치 여부를 확인한다.

```bash
docker --version
```

버전이 정상적으로 나오면 Docker 설치는 건너뛸 수 있다. 설치되어 있지 않다면 다음을 실행한다.

```bash
curl -fsSL \
  https://get.docker.com \
  -o /tmp/get-docker.sh

sudo sh /tmp/get-docker.sh
```

Docker 서비스를 시작한다.

```bash
sudo systemctl enable --now docker
```

WSL에서 systemd를 사용하지 않는다면 다음을 사용한다.

```bash
sudo service docker start
```

설치 확인:

```bash
sudo docker version
sudo docker run --rm hello-world
```

`Hello from Docker!`가 출력되면 정상이다.

### Docker Compose 확인

```bash
docker compose version
```

명령을 찾지 못하면 다음을 설치한다.

```bash
sudo apt-get install -y docker-compose-plugin
```

구형 `docker-compose` 명령도 확인한다.

```bash
command -v docker-compose || true
```

DRfC 스크립트가 구형 명령을 요구하는데 설치되어 있지 않다면 다음을 실행한다.

```bash
sudo apt-get install -y docker-compose
```

---

## 3. NVIDIA Container Toolkit 설치

NVIDIA 저장소 검증키를 등록한다.

```bash
curl -fsSL \
  https://nvidia.github.io/libnvidia-container/gpgkey |
sudo gpg --dearmor --yes \
  -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
```

NVIDIA Container Toolkit 저장소를 등록한다.

```bash
curl -s -L \
  https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list |
sed \
  's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' |
sudo tee \
  /etc/apt/sources.list.d/nvidia-container-toolkit.list
```

패키지 목록을 갱신하고 Toolkit을 설치한다.

```bash
sudo apt-get update
sudo apt-get install -y nvidia-container-toolkit
```

구형 가이드의 `nvidia-docker2` 대신 현재는 `nvidia-container-toolkit`을 사용한다.

---

## 4. Docker에 NVIDIA runtime 등록

DRfC와 호환되도록 NVIDIA runtime을 등록하고 기본 runtime으로 지정한다.

```bash
sudo nvidia-ctk runtime configure \
  --runtime=docker \
  --set-as-default
```

Docker를 재시작한다.

```bash
sudo systemctl restart docker
```

systemd를 사용하지 않는다면 다음을 실행한다.

```bash
sudo service docker restart
```

설정 파일 문법을 확인한다.

```bash
sudo jq . /etc/docker/daemon.json
```

runtime 등록 상태를 확인한다.

```bash
sudo docker info |
grep -iE 'runtimes|default runtime'
```

예상 결과:

```text
Runtimes: io.containerd.runc.v2 nvidia runc
Default Runtime: nvidia
```

---

## 5. 현재 사용자를 docker 그룹에 추가

```bash
sudo usermod -aG docker "$(id -un)"
```

등록 상태를 확인한다.

```bash
getent group docker
```

Ubuntu 터미널을 닫은 후 Windows PowerShell에서 WSL을 완전히 종료한다.

```powershell
wsl --shutdown
```

Ubuntu를 다시 열고 그룹을 확인한다.

```bash
groups
```

출력에 `docker`가 있어야 한다. 이제 `sudo` 없이 Docker를 테스트한다.

```bash
docker ps
docker run --rm hello-world
```

`docker.sock permission denied`가 발생하면 현재 셸에서 다음을 실행한 후 다시 확인한다.

```bash
newgrp docker
```

---

## 6. CUDA 컨테이너 GPU 연결 테스트

DRfC와 같은 CUDA 12.6 계열 이미지로 검사한다.

```bash
docker run --rm \
  --gpus all \
  nvidia/cuda:12.6.3-base-ubuntu24.04 \
  nvidia-smi
```

컨테이너 안에서도 다음 GPU가 보여야 한다.

```text
NVIDIA GeForce RTX 4060 Laptop GPU
```

이 테스트가 성공하면 다음 연결이 모두 정상이라는 뜻이다.

```text
Windows NVIDIA Driver
→ WSL2 GPU 인터페이스
→ Docker Engine
→ NVIDIA Container Toolkit
→ CUDA 컨테이너
```

이 테스트가 실패한 상태에서는 DRfC 설치로 넘어가지 않는다.

---

## 7. Docker Swarm 상태 사전 확인

```bash
docker info |
grep -i swarm
```

최초 설치에서는 다음처럼 나올 수 있다.

```text
Swarm: inactive
```

정상이다. DRfC의 `init.sh`가 Swarm을 구성하므로 직접 `docker swarm init`을 실행하지 않는다.

---

## 8. DRfC 저장소 다운로드

```bash
cd "$HOME"

git clone \
  https://github.com/aws-deepracer-community/deepracer-for-cloud.git

cd "$HOME/deepracer-for-cloud"
```

현재 버전을 확인한다.

```bash
git status
git log -1 --oneline
```

파일 소유권을 확인한다.

```bash
find . \
  -maxdepth 2 \
  ! -user "$(id -un)" \
  -print |
head
```

정상이라면 아무것도 출력되지 않는다.

> `sudo git clone` 또는 `sudo bin/init.sh`를 사용하지 않는다. 프로젝트 파일이 root 소유가 되면 `system.env`, `run.env`, `custom_files` 수정 과정에서 권한 오류가 발생할 수 있다.

---

## 9. DRfC GPU·Local 모드 초기화

WSL에서는 `bin/prepare.sh`를 실행하지 않고 다음 초기화 명령을 사용한다.

```bash
bin/init.sh \
  -a gpu \
  -c local
```

옵션 의미:

- `-a gpu`: GPU 학습 이미지 사용
- `-c local`: AWS S3 대신 로컬 MinIO 사용

초기화는 Docker 이미지 다운로드 때문에 오래 걸릴 수 있다. 다음 오류가 없어야 한다.

```text
Permission denied
Cannot connect to the Docker daemon
NVIDIA runtime not found
no space left on device
```

초기화 완료 후 생성 파일을 확인한다.

```bash
ls -l \
  system.env \
  run.env

ls -l custom_files
```

---

## 10. MinIO 프로필 확인

현재 DRfC의 `bin/init.sh -c local`은 AWS CLI의 `minio` 프로필을 먼저 확인한다.

- 이미 `minio` 프로필이 있으면 그대로 사용한다.
- 프로필이 없으면 임의 Access Key와 Secret Key를 생성한다.
- Region은 `us-east-1`로 설정한다.

따라서 사용자가 직접 `aws configure --profile minio`를 실행하지 않았더라도 다음 메시지와 함께 자동 생성될 수 있다.

```text
Creating default minio credentials in AWS profile 'minio'
```

프로필 목록을 확인한다.

```bash
aws configure list-profiles
```

`minio` 프로필 설정 상태를 확인한다.

```bash
aws configure list \
  --profile minio
```

Access Key와 Secret Key 원문을 터미널이나 로그에 출력하거나 공유하지 않는다.

### `wsl --unregister` 이후에도 예전 프로필이 남을 수 있는가?

일반적인 경우에는 남지 않는다. `wsl --unregister <배포판명>`은 해당 배포판의 가상 디스크와 Linux 홈 디렉터리를 삭제하므로 `/home/<사용자>/.aws/credentials`도 함께 삭제된다.

이번 설치에서 `minio` 프로필이 존재하는 가장 유력한 이유는 현재 버전의 DRfC `init.sh`가 새 자격 증명을 자동 생성했기 때문이다.

예전 설정이 실제로 살아남을 수 있는 예외는 다음과 같다.

- 다른 이름의 WSL 배포판을 unregister하고 기존 배포판은 그대로 둔 경우
- `~/.aws`를 Windows 경로에 심볼릭 링크한 경우
- `AWS_SHARED_CREDENTIALS_FILE` 또는 `AWS_CONFIG_FILE`이 외부 경로를 가리키는 경우
- export/import 또는 백업 복원으로 배포판을 되살린 경우
- Windows의 `.aws` 폴더를 WSL 홈으로 복사한 경우

실제 저장 위치는 다음으로 확인할 수 있다.

```bash
printf 'HOME=%s\n' "$HOME"
printf 'AWS_SHARED_CREDENTIALS_FILE=%s\n' \
  "${AWS_SHARED_CREDENTIALS_FILE:-<unset>}"
printf 'AWS_CONFIG_FILE=%s\n' \
  "${AWS_CONFIG_FILE:-<unset>}"

ls -ld "$HOME/.aws"
readlink -f "$HOME/.aws"
```

일반적인 로컬 저장 위치는 다음과 같다.

```text
/home/<사용자>/.aws/credentials
/home/<사용자>/.aws/config
```

설정 파일의 값은 출력하지 않고 파일 생성 시간과 수정 시간만 확인할 수 있다.

```bash
stat \
  "$HOME/.aws/credentials" \
  "$HOME/.aws/config"
```

이 시간이 이번 `init.sh` 실행 시각과 비슷하면 새로 자동 생성된 것이다.

---

## 11. DRfC 환경 활성화

```bash
cd "$HOME/deepracer-for-cloud"
source bin/activate.sh
```

`dr-*` 명령이 등록되었는지 확인한다.

```bash
type dr-start-training
type dr-upload-custom-files
```

Local 모드 설정을 확인한다.

```bash
grep -nE \
  '^DR_(CLOUD|ARCH|LOCAL_S3_PROFILE|LOCAL_S3_BUCKET)=' \
  system.env run.env
```

GPU 이미지 설정을 확인한다.

```bash
grep -nE \
  '^DR_.*(IMAGE|ARCH)=' \
  system.env run.env
```

GPU 학습 이미지 또는 `gpu` 아키텍처가 선택되어 있어야 한다.

---

## 12. DRfC 기본 인프라 확인

Docker Swarm 상태:

```bash
docker info |
grep -i swarm
```

예상 결과:

```text
Swarm: active
```

노드 확인:

```bash
docker node ls
```

로컬 노드 하나가 `Ready`이고, Manager 상태가 `Leader`여야 한다.

네트워크 확인:

```bash
docker network ls |
grep -E 'sagemaker-local|ingress'
```

DRfC 이미지 확인:

```bash
docker image ls \
  'awsdeepracercommunity/*'
```

---

## 13. 기본 Custom files 검증

첫 학습은 설치 오류와 사용자 코드 오류를 분리하기 위해 DRfC 기본 파일로 실행하는 것을 권장한다.

파일 확인:

```bash
ls -l \
  custom_files/reward_function.py \
  custom_files/model_metadata.json \
  custom_files/hyperparameters.json
```

JSON 문법 검사:

```bash
python3 -m json.tool \
  custom_files/model_metadata.json \
  >/dev/null &&
echo 'model_metadata.json: OK'
```

```bash
python3 -m json.tool \
  custom_files/hyperparameters.json \
  >/dev/null &&
echo 'hyperparameters.json: OK'
```

Reward function Python 문법 검사:

```bash
python3 -m py_compile \
  custom_files/reward_function.py &&
echo 'reward_function.py: OK'
```

세 파일 모두 `OK`가 출력되어야 한다.

---

## 14. MinIO 시작 및 Custom files 업로드

```bash
dr-upload-custom-files
```

이 명령은 일반적으로 다음 작업을 수행한다.

- 로컬 MinIO 서비스 시작
- 버킷 준비
- Reward function 업로드
- Model metadata 업로드
- Hyperparameters 업로드

서비스 상태를 확인한다.

```bash
docker service ls
```

컨테이너 상태를 확인한다.

```bash
docker ps \
  --format \
  'table {{.Names}}\t{{.Status}}\t{{.Image}}'
```

MinIO 서비스가 `1/1` replica 또는 실행 중 상태여야 한다.

---

## 15. 첫 기본 학습 실행

```bash
dr-start-training
```

서비스 상태 확인:

```bash
docker service ls
```

GPU 사용량 확인:

```bash
watch -n 2 nvidia-smi
```

`watch` 종료:

```text
Ctrl+C
```

다른 터미널에서 로그를 확인하려면 먼저 DRfC 환경을 활성화한다.

```bash
cd "$HOME/deepracer-for-cloud"
source bin/activate.sh
```

Sagemaker 로그:

```bash
dr-logs-sagemaker
```

Robomaker 로그:

```bash
dr-logs-robomaker
```

학습을 종료할 때:

```bash
dr-stop-training
```

종료 후 상태 확인:

```bash
docker service ls
docker ps
```

MinIO는 학습 데이터 저장용 서비스이므로 학습 종료 후에도 실행 중이거나 Swarm에 의해 재생성될 수 있다. 삭제하려면 단일 컨테이너가 아니라 해당 서비스 또는 stack을 기준으로 관리해야 한다.

---

## 최종 성공 기준

| 단계 | 성공 기준 |
|---|---|
| WSL GPU | `nvidia-smi`에서 RTX 4060 표시 |
| Docker | `docker run --rm hello-world` 성공 |
| NVIDIA runtime | `docker info`에 `nvidia` 표시 |
| GPU container | CUDA 컨테이너에서 RTX 4060 표시 |
| DRfC 초기화 | `system.env`, `run.env` 생성 |
| Docker Swarm | `Swarm: active`, 노드 `Ready` |
| Custom files | JSON·Python 검사 모두 `OK` |
| MinIO | 서비스 replica `1/1` |
| Training | Sagemaker·Robomaker 실행 및 GPU 사용 확인 |

## 하지 말아야 할 작업

- WSL에 `nvidia-driver-*` 설치
- WSL에 `cuda-drivers` 설치
- `sudo bin/init.sh` 실행
- WSL에서 `bin/prepare.sh` 실행
- GPU 컨테이너 검증 실패 상태에서 DRfC 설치 강행
- 첫 검증부터 커스텀 Reward function 사용
- 오류 발생 직후 `docker system prune -a` 실행
- Swarm 서비스가 관리하는 MinIO 컨테이너만 반복 삭제

## 참고 문서

- NVIDIA CUDA on WSL User Guide: <https://docs.nvidia.com/cuda/wsl-user-guide/index.html>
- NVIDIA Container Toolkit 설치: <https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html>
- DRfC 공식 저장소: <https://github.com/aws-deepracer-community/deepracer-for-cloud>
- DRfC 설치 안내: <https://aws-deepracer-community.github.io/deepracer-for-cloud/installation.html>
