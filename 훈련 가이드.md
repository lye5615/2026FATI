### 1. 리눅스 CLI 기본 제어

- **핵심 개념**: 터미널 기반의 디렉터리 이동(`cd`), 목록 확인(`ls`), 파일 편집(`vi`/`nano`)을 통한 코드 수정 역량

    - 터미널은 마우스가 없는 환경임을 상기시키고 3대 필수 동작을 1:1로 실습시킵니다.
    - **이동과 탐색**: `cd <경로>`(폴더 이동), `cd ..`(상위 폴더), `ls -l`(파일 목록 및 권한 확인)
        '-'   일반 파일
        'd'   디렉터리(directory)
        'l'   심볼릭 링크(symbolic link)

        rwx | r-x | r-x
        소유자 | 그룹 | 기타 사용자

        r = read    읽기
        w = write   쓰기/수정
        x = execute 실행
        - = 해당 권한 없음


    - **편집기 기초**: `vi <파일명>` 진입 후 `i`를 눌러 **입력 모드**로 변경 → 수정 완료 후 `Esc` 누르고 `:wq` 입력 후 엔터로 **저장 및 종료**.

### 2. 강화학습(RL) 기반 자율주행 원리

- **핵심 개념**: 에이전트(차량)가 환경(트랙)에서 상태를 관측하고, 행동을 취하며, 환경으로부터 보상을 받아 최적의 정책(Policy)을 완성해 가는 루프

    - **강아지 훈련 비유**: 정답 사진을 미리 주는 것(지도학습)이 아니라, 강아지(차량)가 트랙을 달리다 잘하면 간식(높은 보상)을 주고 이탈하면 혼을 내서(낮은 보상/종료) **"어떻게 달려야 간식을 가장 많이 받는지" 스스로 깨닫게 만드는 과정**임을 설명합니다.

### 3. 핵심 구성 파일 3종 (`custom_files`)

- **핵심 개념**: 모델 행동 양식을 결정하는 3대 파일의 역할 분담 이해
    - `reward_function.py`: 보상 함수 (채점 기준)
    - `model_metadata.json`: 액션 스페이스 (차량의 행동 선택지)
        - `hyperparameters.json`: 강화학습 최적화 세부 설정


    - **`reward_function.py` = 점수 채점표** : 우리의 차량이 어떤 동작을 했을때 점수를 얼마나 줄 것인지에 대해서 정해주는 파일입니다.
    - **`model_metadata.json` = 차량의 선택지**: "너는 좌우 조향각 몇 도, 속도 몇 m/s 으로만 움직일 수 있어"라고 차량의 물리적 행동 반경을 정해주는 규격입니다.
    - **`hyperparameters.json` = 학습 전략**: 한 번에 몇 문제씩 몰아서 공부할지(Batch size), 공부한 내용을 얼마나 적극적으로 반영할지(Learning rate)를 결정하는 수험 전략서입니다.

### 4. 환경 갱신 및 파일 반영 명령어

- **핵심 개념**: 파일 수정 영역에 따른 반영 명령어(`dr-upload-custom-files` vs `dr-update-env`)의 차이
    - 상태(카메라/센서) → 행동(조향/속도) → 보상(점수 계산) → 신경망 가중치 갱신의 순환 구조를 시각적으로 짚어줍니다.

    - **코드/설정 파일 변경 시**: `dr-upload-custom-files` → 로컬 파일들을 S3/MinIO 버킷으로 쏴주는 명령어입니다. (이걸 안 치면 코드를 고쳐도 이전 코드로 학습함)
    - **시스템 환경 변수(`.env`) 변경 시**: `dr-update-env` → 도커 스택과 셸 환경 설정을 새로 빌드하여 동기화하는 명령어입니다.

### 5. 모델 덮어쓰기 방지 및 이력 관리

- **핵심 개념**: `dr-start-training -w`의 위험성(-w는 wipe) 인지 및 체크포인트 보존법

    - `w` 플래그는 기존 버킷의 모델 데이터를 완전히 포맷(Wipe)하고 0스텝부터 새로 시작하는 옵션입니다.
    - **해결책**: `nrun.env`에서 `DR_LOCAL_S3_MODEL_PREFIX`(또는 모델명)의 이름을 변경하여 새로운 폴더에 저장되게 하거나, `data/minio/bucket` 내 기존 모델 폴더명을 미리 변경(백업)하도록 지도합니다.

### 6. 보상 함수 파라미터(Parameter) 조합

- **핵심 개념**: 텔레메트리 파라미터(차량 상태 데이터)를 조합한 복합 보상 설계

    - 파라미터는 시뮬레이터 안의 차량에 부착된 가상 센서 측정값입니다.
    - `all_wheels_on_track`(바퀴 이탈 여부), `distance_from_center` & `track_width`(중앙선으로부터의 거리 비율), `speed`(속도), `steering_angle`(조향각), `heading` & `waypoints`(트랙 방향과의 정렬도) 등을 나열해 줍니다.
    - 단일 파라미터만 주면 차량이 이상 행동(예: 속도 보상만 주면 직진 후 벽 충돌, 중앙선 보상만 주면 거북이 주행)을 하므로 가중치를 곱해 균형을 맞춰야 함을 설명합니다.

### 7. 도커 컨테이너 로그 추적 기법

- **핵심 개념**: `docker ps`, `docker logs -f` 및 SageMaker(`/train`)와 RoboMaker(`/opt`) 로그의 분리 추적

    - `docker ps` 입력 후 나오는 컨테이너 목록 중 이름/커맨드를 확인시킵니다.
    - **`/train` 컨테이너 (SageMaker)**: 신경망 두뇌가 가중치를 업데이트하는 곳입니다. 실시간 로스(Loss), 학습 진행 단계가 찍힙니다.
    - **`/opt` 또는 `simapp` 컨테이너 (RoboMaker)**: 가상 트랙 주행 현황입니다. 차량의 완주율(Completion %), 랩타임, 스텝 수, 보상 점수가 실시간으로 찍힙니다.
    - 실시간 모니터링: `docker logs -f <컨테이너ID>` (종료는 `Ctrl + C`)

### 8. RL 메트릭 해석 (KL Divergence & Entropy)

- **핵심 개념**: 정책 수렴도와 탐색 수준을 나타내는 메트릭 분석 및 오버피팅/정책 붕괴 방지

    - **엔트로피(Entropy)**: **차량의 호기심(무작위 탐색 수준)**
        - 수치가 높음: "이것저것 다양하게 시도해보는 상태"
        - 수치가 점점 낮아짐: "특정 주행 방식이 최선이라고 확신하고 굳어지는 상태 (정상 수렴)"
        - 너무 일찍 0이 됨: 조기 수렴(Local Minimum)에 빠져 트랙 중간에서 이상한 짓을 반복하므로 학습을 중단하고 보상함수 수정 필요
    - **KL 다이버전스(KL Divergence)**: **이전 에포크 대비 정책의 변화 폭**
        - 적정 범위에서 서서히 안정되어야 정상적인 학습입니다.
        - 값이 갑자기 급등(Spike)하면: 이전까지 잘 배운 주행법을 잊어버리고 뇌가 망가진 상태(Policy Collapse)이므로 직전 체크포인트로 롤백해야 함을 안내합니다.