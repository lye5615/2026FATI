- 주의: 환경설정0까지 완료한 뒤 아래 절차를 따르도록 합니다.

# Docker Desktop 설치

이미 해당 노트북에서 Docker Desktop을 사용하고 있다면:

> `기존 Docker Desktop을 사용하는 경우`

로 이동한다.

---

## 1-1. Docker를 처음 설치하는 경우

Docker Desktop을 설치한다.

권장 설정:

```text

Per-user installation

Use WSL 2 instead of Hyper-V

Allow Windows Containers → 체크하지 않음

```

설치 후 Docker Desktop을 실행한다.

다음 메뉴로 이동한다.

```text

Settings

→ Resources

→ WSL Integration

```

다음을 활성화한다.

```text

Enable integration with my default WSL distro → ON

Ubuntu-22.04 → ON

```

이후:

```text

Apply & Restart

```

를 누른다.

PowerShell에서 WSL을 다시 시작한다.

```powershell

wsl --shutdown

wsl -d Ubuntu-22.04

```

Ubuntu에서 Docker를 확인한다.

```bash

docker --version

```

테스트:

```bash

docker run --rm hello-world

```

다음 문구가 나오면 정상이다.

```text

Hello from Docker!

```

---

## 에러 슈팅: Docker 명령어가 없는 경우

다음과 같은 오류가 나오면:

```text

docker: command not found

```

또는:

```text

The command 'docker' could not be found in this WSL 2 distro.

```

Docker Desktop과 Ubuntu의 WSL Integration 문제일 가능성이 높다.

> 자세한 해결 방법: `에러슈팅.md`의 **1. docker: command not found** 참고

---

# 1-2. 기존 Docker Desktop을 사용하는 경우

이미 Docker Desktop이 설치되어 있다면 Ubuntu 내부에 Docker Engine을 새로 설치할 필요가 없다.

먼저 Docker Desktop을 실행한다.

다음 메뉴를 확인한다.

```text

Settings

→ Resources

→ WSL Integration

```

다음이 ON인지 확인한다.

```text

Ubuntu-22.04

```

Ubuntu에서:

```bash

docker --version

docker info

```

두 명령이 정상적으로 실행되면 바로 다음 단계로 넘어간다.

---

# 2. 필요한 패키지 설치

Ubuntu 22.04에서 실행한다.

```bash

sudo apt update

sudo apt install -y git jq awscli python3-boto3

```

설치 확인:

```bash

git --version

jq --version

aws --version

```

Docker Compose 확인:

```bash

docker compose version

```
---
# 3. DeepRacer for Cloud 설치

Ubuntu 홈 폴더로 이동한다.

```bash

cd ~

```

DRfC 저장소를 복제한다.

```bash

git clone https://github.com/aws-deepracer-community/deepracer-for-cloud.git

```

DRfC 폴더로 이동한다.

```bash

cd ~/deepracer-for-cloud

```

CPU + Local MinIO 환경으로 초기화한다.

```bash

bin/init.sh -a cpu -c local

```

또는:

```bash

./bin/init.sh -a cpu -c local

```

---

# 4. Docker Swarm 초기화 확인

초기화 중 다음과 같은 메시지가 나오면 정상이다.

```text

Swarm initialized: current node (...) is now a manager.

```

확인:

```bash

docker node ls

```

정상적인 상태:

```text

STATUS          Ready

AVAILABILITY    Active

MANAGER STATUS  Leader

```

---

## 에러 슈팅: Swarm IP 오류가 발생하는 경우

다음 오류가 잠시 나타날 수 있다.

```text

could not choose an IP address to advertise since this system has multiple addresses

```

그 뒤 자동 재시도 후:

```text

Swarm initialized

```

가 나오고:

```bash

docker node ls

```

결과가 정상이라면 문제없다.

계속 실패한다면:

> `에러슈팅.md`의 **3. Docker Swarm IP 오류** 참고

---

# 5. MinIO 확인

DRfC Local 환경에서는 MinIO가 S3 역할을 한다.

확인:

```bash

docker service ls

```

정상 예시:

```text

NAME       REPLICAS

s3_minio   1/1

```

다음처럼 나오면 정상이다.

```text

s3_minio   1/1

```

---

# 6. DRfC 환경 활성화

DRfC 폴더에서:

```bash

cd ~/deepracer-for-cloud

source bin/activate.sh

```

환경 확인:

```bash

dr-summary

```

정상 예시:

```text

Docker style      swarm

Cloud / Bucket    local bucket

Workers           1

Race type         TIME_TRIAL

World / track     reinvent_base

```

---

# 팁: dr-* 명령어가 안 먹으면

`dr-summary`, `dr-start-training`, `dr-stop-training` 등 `dr-*` 명령어가 인식되지 않는다면 가장 먼저 실행한다.

```text

→ cd ~/deepracer-for-cloud

→ source bin/activate.sh

→ 다시 실행

```

자세한 내용:

> `에러슈팅.md`의 **4. dr-* 명령어가 인식되지 않는 경우** 참고