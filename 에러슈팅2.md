# DeepRacer for Cloud 에러 정리

> **주의**
>
> 아래 해결 방법 중 `purge`, `rm -rf`, `docker system prune`, `wsl --unregister` 등은 기존 설정이나 데이터를 삭제할 수 있다.
> 특히 마지막의 **완전 초기화 방법**은 필요한 파일을 반드시 백업한 뒤 사용한다.

# 목차

## 1. 환경설정 관련 에러 모음

### 1.1 `aws configure --profile minio` 실행 시 OpenSSL 오류
### 1.2 `bin/init.sh -a gpu -c local` 실행 시 `Permission denied`
### 1.3 패키지 설치 중 `pkgProblemResolver::Resolve generated breaks`
### 1.4 `bin/prepare.sh` 실행 중 Docker Compose 패키지 충돌

## 2. 샘플 코드 학습 관련 에러 모음

### 2.1 `source bin/activate.sh` 실행 시 `No such file or directory`
### 2.2 `source bin/activate.sh` 실행 중 `This node is not a swarm manager`
### 2.3 `source bin/activate.sh` 이후 `command not found`
### 2.4 `~/deepracer-for-cloud`에서 `ls`를 실행했는데 아무것도 보이지 않는 경우
### 2.5 `dr-update` 실행 시 `syntax error near unexpected token 'newline'`
### 2.6 `dr-start-training` 후 RoboMaker 컨테이너가 종료되는 문제
### 2.7 `dr-start-training` 실행 시 `expr: non-integer argument`

## 3. 뭔가 많이 꼬였을 때 초기화하는 방법

### 3.1 Ubuntu WSL 자체를 완전히 초기화
### 3.2 Ubuntu는 유지하고 DRfC 관련 환경만 초기화

## 4. 초기화 후 다시 시작할 때

## 5. 빠른 점검 명령어

---

# 1. 환경설정 관련 에러 모음

## 1.1 `aws configure --profile minio` 실행 시 OpenSSL 오류

### 실행 명령어

```bash
aws configure --profile minio
```

### 에러 내용

대표적으로 다음과 같은 오류가 발생한다.

```text
/usr/lib/python3/dist-packages/OpenSSL/crypto.py:12:
CryptographyDeprecationWarning: Python 3.8 is no longer supported ...

Traceback (most recent call last):
  File "/usr/bin/aws", line 19, in <module>
    import awscli.clidriver
...
  File "/usr/lib/python3/dist-packages/OpenSSL/crypto.py", line 1571, in X509StoreFlags
    NOTIFY_POLICY = _lib.X509_V_FLAG_NOTIFY_POLICY

AttributeError: module 'lib' has no attribute 'X509_V_FLAG_NOTIFY_POLICY'
```

### 원인

Ubuntu 패키지 관리자로 설치된 구버전 AWS CLI와 Python/OpenSSL 관련 패키지의 버전 충돌로 인해 발생할 수 있다.

### 해결 방법

기존 AWS CLI를 제거한다.

```bash
sudo apt remove awscli -y
sudo apt autoremove -y
```

AWS CLI v2를 다운로드한다.

```bash
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
```

`unzip`이 없다면 설치한다.

```bash
sudo apt install unzip -y
```

압축 해제:

```bash
unzip awscliv2.zip
```

설치:

```bash
sudo ./aws/install
```

설치 확인:

```bash
aws --version
```

### `aws --version`도 정상적으로 실행되지 않는 경우

`sudo ./aws/install` 실행 결과에 표시된 실제 설치 경로를 확인한다.

예:

```bash
/usr/local/bin/aws --version
```

MinIO 프로필 설정도 실제 경로를 직접 지정하여 실행할 수 있다.

```bash
/usr/local/bin/aws configure --profile minio
```

---

## 1.2 `bin/init.sh -a gpu -c local` 실행 시 `Permission denied`

### 실행 명령어

```bash
bin/init.sh -a gpu -c local
```

### 에러 내용

예:

```text
Detected 1 GPU(s) inside docker.

ln: failed to create symbolic link
'/home/xytron/deepracer-for-cloud/docker/volumes/.aws':
Permission denied

cp: cannot create regular file
'/home/xytron/deepracer-for-cloud/custom_files/hyperparameters.json':
Permission denied

cp: cannot create regular file
'/home/xytron/deepracer-for-cloud/system.env':
Permission denied

cp: cannot create regular file
'/home/xytron/deepracer-for-cloud/run.env':
Permission denied

sed: couldn't open temporary file ...:
Permission denied
```

### 원인

`deepracer-for-cloud` 디렉터리나 내부 파일의 소유자가 현재 Ubuntu 사용자 계정이 아니라 `root` 또는 다른 사용자로 설정되어 있을 가능성이 있다.

### 해결 방법

먼저 DeepRacer for Cloud 디렉터리로 이동한다.

```bash
cd ~/deepracer-for-cloud
```

현재 위치를 확인한다.

```bash
pwd
```

출력된 경로를 `DRFC_DIR`에 넣는다.

예:

```bash
DRFC_DIR=/home/사용자이름/deepracer-for-cloud
```

소유권을 확인한다.

```bash
ls -ld "$DRFC_DIR"
ls -ld "$DRFC_DIR/docker/volumes"
ls -l "$DRFC_DIR/system.env" "$DRFC_DIR/run.env" 2>/dev/null
```

현재 사용자에게 소유권을 부여한다.

```bash
sudo chown -R "$(id -un):$(id -gn)" "$DRFC_DIR"
chmod -R u+rwX "$DRFC_DIR"
```

이후 다시 초기화를 시도한다.

```bash
bin/init.sh -a gpu -c local
```

---

## 1.3 패키지 설치 중 `pkgProblemResolver::Resolve generated breaks`

### 에러 내용

```text
pkgProblemResolver::Resolve generated breaks
```

### 원인

Docker 관련 패키지나 APT 저장소, 의존성 관계가 충돌한 경우 발생할 수 있다.

### 해결 방법

> **주의**
>
> 아래 과정은 기존 Docker 패키지, 설정, 이미지 및 일부 데이터를 제거할 수 있다.
> Docker를 다른 프로젝트에서도 사용하는 환경이라면 먼저 필요한 데이터를 백업한다.

### 1단계. Docker 서비스 중지 및 관련 패키지 삭제

```bash
sudo service docker stop 2>/dev/null || true
```

Docker 및 관련 패키지 삭제:

```bash
sudo apt-get purge -y \
docker-ce \
docker-ce-cli \
containerd.io \
docker-buildx-plugin \
docker-compose \
docker-ce-rootless-extras \
jq \
awscli \
python3-boto3
```

미사용 의존성 패키지 삭제:

```bash
sudo apt-get autoremove --purge -y
sudo apt-get autoclean
```

### 2단계. Docker 잔여 데이터 및 설정 삭제

```bash
sudo rm -rf /var/lib/docker
sudo rm -rf /var/lib/containerd
sudo rm -rf /etc/docker
rm -rf ~/.docker
```

### 3단계. APT 저장소 및 캐시 정리

Docker 저장소 리스트 삭제:

```bash
sudo rm -f /etc/apt/sources.list.d/docker*.list
```

APT 패키지 목록 캐시 삭제:

```bash
sudo rm -rf /var/lib/apt/lists/*
```

### 4단계. APT 패키지 관리자 복구

```bash
sudo apt-get update
sudo apt-get --fix-broken install -y
sudo apt-get upgrade -y
```

이 단계까지 오류 없이 완료되면 패키지 의존성이 상당 부분 복구된 것이다.

### 5단계. DeepRacer 프로젝트 환경 초기화

DeepRacer for Cloud 디렉터리에서 실행한다.

```bash
cd ~/deepracer-for-cloud
```

가상환경 및 설정 파일 삭제:

```bash
rm -rf .venv
rm -f system.env run.env
```

필요하다면 이후 다시 `prepare.sh` 및 초기화 절차를 수행한다.

---

## 1.4 `bin/prepare.sh` 실행 중 Docker Compose 패키지 충돌

### 에러 내용

```text
Unpacking docker-compose-v2 (2.40.3+ds1-0ubuntu1~24.04.1) ...

dpkg: error processing archive
/tmp/apt-dpkg-install-W5MyBL/10-docker-compose-v2_2.40.3+ds1-0ubuntu1~24.04.1_amd64.deb
(--unpack):

trying to overwrite
'/usr/libexec/docker/cli-plugins/docker-compose',
which is also in package
docker-compose-plugin 5.3.1-1~ubuntu.24.04~noble

dpkg-deb: error: paste subprocess was killed by signal (Broken pipe)

E: Sub-process /usr/bin/dpkg returned an error code (1)
```

### 원인

`docker-compose-v2`와 기존 `docker-compose-plugin`이 동일한 파일을 설치하려고 하면서 충돌한 경우이다.

### 해결 방법

#### 1단계. 충돌 패키지 삭제

```bash
sudo apt-get purge -y docker-compose-plugin
```

멈춰 있던 패키지 설치를 복구한다.

```bash
sudo apt-get -o Dpkg::Options::="--force-overwrite" install -f
```

#### 2단계. `prepare.sh` 재실행

```bash
bin/prepare.sh
```

---

# 2. 샘플 코드 학습 관련 에러 모음

## 2.1 `source bin/activate.sh` 실행 시 `No such file or directory`

### 에러 내용

```bash
source bin/activate.sh
```

실행 시:

```text
bash: bin/activate.sh: No such file or directory
```

### 원인

현재 터미널 위치가 `deepracer-for-cloud` 디렉터리가 아니기 때문이다.

### 해결 방법

```bash
cd ~/deepracer-for-cloud
source bin/activate.sh
```

`bin/activate.sh`는 DeepRacer for Cloud 저장소 내부에 있으므로 해당 폴더에서 실행해야 한다.

---

## 2.2 `source bin/activate.sh` 실행 중 `This node is not a swarm manager`

### 에러 내용

```text
Error response from daemon:
This node is not a swarm manager.

Use "docker swarm init" or "docker swarm join"
to connect this node to swarm and try again.
```

추가로 다음 메시지가 나타날 수 있다.

```text
bash: : : integer expression expected

Since --detach=false was not specified,
tasks will be created in the background.
```

### 원인

Docker Swarm이 정상적으로 초기화되지 않았거나 현재 노드가 Swarm Manager 상태가 아닌 경우이다.

### 해결 방법

먼저 Swarm 초기화를 시도한다.

```bash
docker swarm init
```

### 이미 Swarm에 포함되어 있다는 오류가 발생하는 경우

```text
Error response from daemon:
This node is already part of a swarm.
Use "docker swarm leave" to leave this swarm and join another one.
```

기존 Swarm에서 나간다.

```bash
docker swarm leave --force
```

다시 초기화한다.

```bash
docker swarm init
```

### IP 주소를 자동으로 선택하지 못하는 경우

다음과 같은 오류가 발생할 수 있다.

```text
Error response from daemon:
could not choose an IP address to advertise since this system
has multiple addresses on different interfaces
(10.255.255.254 on lo and 172.25.242.56 on eth0)
- specify one with --advertise-addr
```

이 경우 사용할 네트워크 인터페이스의 IP를 지정하여 Swarm을 초기화한다.

예:

```bash
docker swarm init --advertise-addr <WSL의 eth0 IP>
```

WSL의 IP는 다음처럼 확인할 수 있다.

```bash
ip addr show eth0
```

초기화 후 확인:

```bash
docker node ls
```

정상 상태 예:

```text
STATUS          Ready
AVAILABILITY    Active
MANAGER STATUS  Leader
```

---

## 2.3 `source bin/activate.sh` 이후 `command not found`

### 증상

환경 활성화 과정 이후 `dr-*` 명령어가 정상적으로 등록되지 않거나 `command not found`가 발생한다.

### 우선 해결 방법

Ubuntu 터미널을 완전히 종료한 뒤 다시 실행한다.

이후:

```bash
cd ~/deepracer-for-cloud
source bin/activate.sh
```

확인:

```bash
type dr-summary
type dr-start-training
```

정상이라면 함수로 표시된다.

---

## 2.4 `~/deepracer-for-cloud`에서 `ls`를 실행했는데 아무것도 보이지 않는 경우

### 에러 내용

```bash
cd ~/deepracer-for-cloud
ls
```

실행 후 예상한 파일 목록이 보이지 않는다.

### 해결 방법

경로 상태를 다시 확인한다.

```bash
pwd
ls -la
```

기존에 다음과 같이 디렉터리를 이동했다가 정상화된 사례가 있었다.

```bash
cd bin
cd ..
```

이후 다시:

```bash
pwd
ls -la
```

확인한다.

> 단순히 `cd bin && cd ..` 자체가 파일을 복구하는 것은 아니다.
> 실제로 파일이 보이지 않는다면 현재 경로, 마운트 상태, 저장소 clone 여부를 함께 확인해야 한다.

---

## 2.5 `dr-update` 실행 시 `syntax error near unexpected token 'newline'`

### 에러 내용

```text
bash: syntax error near unexpected token 'newline'
```

### 원인

`system.env` 또는 `run.env`의 값에 `< >`, 공백, 괄호 등의 특수문자가 들어간 경우 shell의 `eval` 과정에서 문법 오류가 발생할 수 있다.

예:

```text
DR_DOCKER_STYLE=<DOCKER_STYLE>
```

### 해결 방법

환경 파일에서 특수문자가 포함된 항목을 찾는다.

```bash
grep -n -E '=.*[ ()&|<>#]' system.env run.env 2>/dev/null
```

잘못된 값을 수정한다.

예:

```bash
sed -i 's/DR_DOCKER_STYLE=<DOCKER_STYLE>/DR_DOCKER_STYLE=swarm/' system.env
```

수정 후 환경을 다시 적용한다.

```bash
source bin/activate.sh
dr-update-env
```

---

## 2.6 `dr-start-training` 후 RoboMaker 컨테이너가 종료되는 문제

### 증상

`dr-start-training`을 실행하면 일정 시간 후 RoboMaker 컨테이너가 종료되고 학습이 진행되지 않는다.

로그에서 다음 오류가 나타난다.

```text
[RTPS_TRANSPORT_SHM Error]
Failed init_port fastrtps_port7000:
open_and_lock_file failed
```

이후:

```text
DEAD NODES DETECTED!
```

가 출력되며 RoboMaker가 종료될 수 있다.

컨테이너 종료 상태:

```text
ExitCode=143
OOMKilled=false
```

### 원인

RoboMaker 내부 ROS 2 노드가 사용하는 Fast DDS의 공유 메모리(SHM) 통신에서 포트 잠금 오류가 발생한 사례이다.

NodeMonitor가 `/download_params_and_roslaunch_agent_node`의 응답을 받지 못해 해당 노드를 비정상 상태로 판단하고 컨테이너를 종료하게 된다.

### 확인 방법

RoboMaker 컨테이너 ID를 확인한다.

```bash
docker ps -a
```

그다음:

```bash
docker inspect <ROBOMAKER_CONTAINER_ID> --format \
'Name={{.Name}}
Image={{.Config.Image}}
Status={{.State.Status}}
ExitCode={{.State.ExitCode}}
OOMKilled={{.State.OOMKilled}}
Error={{.State.Error}}
StartedAt={{.State.StartedAt}}
FinishedAt={{.State.FinishedAt}}'
```

다음과 비슷한 결과가 나오고 로그에 `DEAD NODES DETECTED!`가 있다면 해당 문제를 의심할 수 있다.

```text
ExitCode=143
OOMKilled=false
```

### 해결 방법

`docker/docker-compose-training.yml`의 `robomaker.environment`에 다음 환경변수를 추가한다.

```yaml
- FASTDDS_BUILTIN_TRANSPORTS=UDPv4
```

이는 Fast DDS가 문제가 발생한 SHM 대신 UDPv4 transport를 사용하도록 하는 설정이다.

설정 후 기존 학습을 종료한다.

```bash
dr-stop-training
```

다시 학습을 시작한다.

```bash
dr-start-training
```

해당 사례에서는 이후 SHM 포트 잠금 오류 및 `DEAD NODES DETECTED` 오류가 사라지고 학습이 정상적으로 진행되었다.

---

## 2.7 `dr-start-training` 실행 시 `expr: non-integer argument`

### 에러 내용

```text
dr-start-training

expr: non-integer argument
expr: non-integer argument
expr: non-integer argument
```

### 원인

학습 또는 평가 시간을 나타내는 필수 환경변수가 비어 있거나 숫자가 아닌 값으로 설정된 경우 발생할 수 있다.

### 해결 방법

`run.env` 또는 사용하는 환경 설정 파일에서 다음 값을 확인한다.

```bash
DR_TRAIN_MINUTES=60
DR_EVAL_MINUTES=15
```

값은 정수 형태로 입력한다.

수정 후:

```bash
dr-update-env
```

또는 필요한 경우:

```bash
source bin/activate.sh
dr-update-env
```

다시 학습을 시작한다.

```bash
dr-start-training
```

---

# 3. 뭔가 많이 꼬였을 때 초기화하는 방법

일부 오류는 패키지, Docker, WSL, DRfC 설정이 여러 번 변경되면서 원인을 찾기 어려운 상태가 될 수 있다.

이 경우 다음 두 가지 방법 중 하나를 사용할 수 있다.

```text
방법 1. Ubuntu WSL 자체를 완전히 초기화
방법 2. Ubuntu는 유지하고 DRfC 관련 환경만 삭제
```

---

## 3.1 Ubuntu WSL 자체를 완전히 초기화

> **매우 주의**
>
> `wsl --unregister`를 실행하면 해당 Ubuntu 배포판 안의 파일, 프로그램, 설정이 모두 삭제된다.
> 복구가 필요할 수 있는 파일이 있다면 반드시 먼저 백업한다.

### 1단계. 배포판 이름 확인

Windows PowerShell에서:

```powershell
wsl -l -v
```

예:

```text
NAME            STATE     VERSION
Ubuntu-22.04    Running   2
```

---

### 2단계. 필요한 경우 Ubuntu 전체 백업

```powershell
wsl --export Ubuntu-22.04 D:\ubuntu-backup.tar
```

이 명령으로 현재 Ubuntu 배포판을 tar 파일로 백업할 수 있다.

---

### 3단계. WSL 종료

```powershell
wsl --shutdown
```

---

### 4단계. Ubuntu 배포판 삭제

```powershell
wsl --unregister Ubuntu-22.04
```

이 명령을 실행하면 해당 Ubuntu 안의 데이터가 삭제된다.

예를 들면 다음 항목들이 영향을 받을 수 있다.

```text
deepracer-for-cloud
Ubuntu 내부에 설치된 프로그램
MinIO 관련 로컬 데이터
AWS CLI 설정
~/.aws
Python 환경
apt로 설치한 패키지
DRfC system.env
DRfC run.env
각종 shell 환경변수 및 설정
```

> Docker Desktop이 관리하는 이미지·컨테이너·볼륨의 실제 삭제 범위는 Docker Desktop 구성과 데이터 저장 위치에 따라 다를 수 있으므로 별도로 확인한다.

---

### 5단계. 설치 가능한 Ubuntu 확인

```powershell
wsl --list --online
```

---

### 6단계. Ubuntu 22.04 재설치

```powershell
wsl --install -d Ubuntu-22.04
```

이후 완전히 새로운 Ubuntu 환경에서 DeepRacer for Cloud 설치를 다시 진행한다.

---

## 3.2 Ubuntu는 유지하고 DRfC 관련 환경만 초기화

다른 프로젝트 때문에 Ubuntu 전체를 삭제할 수 없다면 이 방법을 사용한다.

> **주의**
>
> Docker가 다른 프로젝트와 공유되고 있다면 아래 Docker 정리 명령으로 다른 프로젝트의 컨테이너, 이미지, 볼륨까지 삭제될 수 있다.

### 1단계. Docker 컨테이너 제거

실행 중이거나 종료된 모든 컨테이너를 삭제하려면:

```bash
docker ps -aq | xargs -r docker rm -f
```

---

### 2단계. 사용하지 않는 Docker 리소스 정리

```bash
docker system prune -a --volumes -f
```

이 명령은 사용되지 않는 다음 항목을 제거할 수 있다.

```text
containers
networks
images
volumes
```

특히 `--volumes`를 사용하면 Docker volume도 정리 대상이 된다.

---

### 3단계. Docker Swarm 종료

DeepRacer for Cloud에서 Swarm을 사용했다면:

```bash
docker swarm leave --force
```

---

### 4단계. DeepRacer for Cloud 저장소 삭제

```bash
rm -rf ~/deepracer-for-cloud
```

다른 위치에 clone한 경우에는 먼저 찾는다.

```bash
find ~ -maxdepth 3 -type d -name "deepracer-for-cloud" 2>/dev/null
```

경로를 확인한 뒤 필요한 저장소만 삭제한다.

---

### 5단계. MinIO 데이터 확인

DRfC local mode에서는 MinIO 관련 데이터가 프로젝트 저장소 내부의 `docker/volumes` 등에 저장될 수 있다.

따라서 해당 저장소 및 volume을 삭제하면 기존 학습 결과도 함께 삭제될 수 있다.

제출할 모델이나 checkpoint가 필요하다면 반드시 먼저 백업한다.

---

### 6단계. AWS CLI 프로필 확인

```bash
aws configure list-profiles
```

예:

```text
default
minio
```

AWS 설정 전체가 DRfC에서만 사용되었다면:

```bash
rm -rf ~/.aws
```

로 삭제할 수 있다.

> AWS CLI를 다른 프로젝트에서도 사용한다면 `~/.aws` 전체를 삭제하지 않는다.
> 필요한 `minio` 프로필만 직접 수정하거나 제거한다.

---

### 7단계. Shell 설정에 남은 DRfC 설정 확인

```bash
grep -nEi 'deepracer|DR_|activate\.sh' \
    ~/.profile ~/.bashrc ~/.bash_profile 2>/dev/null
```

예를 들어 다음과 같은 설정이 남아 있는지 확인한다.

```bash
source ~/deepracer-for-cloud/bin/activate.sh
export DRFC_WSL_IP=...
export DR_LOCAL_S3_ENDPOINT_URL=...
```

불필요한 항목이 있다면 해당 shell 설정 파일에서 직접 삭제한다.

---

# 4. 초기화 후 다시 시작할 때

환경을 초기화한 뒤에는 기존 오류 해결 명령을 무작정 반복하기보다는 처음 설치 순서대로 다시 진행하는 것이 좋다.

기본적으로 다음 순서로 확인한다.

```text
① 사용할 Ubuntu 버전 확인

② Docker Desktop 실행 및 WSL Integration 확인

③ docker info 정상 여부 확인

④ DeepRacer for Cloud 저장소 설치

⑤ prepare.sh 실행

⑥ AWS CLI 및 MinIO profile 설정

⑦ init.sh 실행

⑧ Docker Swarm / MinIO 상태 확인

⑨ source bin/activate.sh

⑩ dr-summary로 최종 환경 확인

⑪ custom_files 업로드

⑫ 학습 시작
```

---

# 5. 빠른 점검 명령어

문제가 생겼을 때 우선 다음 명령어를 순서대로 확인한다.

```bash
cd ~/deepracer-for-cloud
```

```bash
docker --version
```

```bash
docker info
```

```bash
docker node ls
```

```bash
docker service ls
```

```bash
source bin/activate.sh
```

```bash
dr-summary
```

Docker Swarm 정상 상태:

```text
Ready
Active
Leader
```

MinIO 정상 상태:

```text
s3_minio   1/1
```

학습 서비스가 실행되고 있지 않을 때는 일반적으로 MinIO만 남아 있어도 된다.
